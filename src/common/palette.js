// CJS so tailwind.config.js can require it. Single source of truth for hex values.
// App code should import { Colors } from './colors' instead of this file.
module.exports = {
  primary: '#007AFF',
  white: '#fff',
  black: '#000',
  transparent: 'transparent',
};
